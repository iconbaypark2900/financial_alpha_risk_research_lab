# V0 — Trustworthy Backtests. Everything here is imported by code in this
# repository, or is the engine the PRD names. Nothing else belongs in this file.
#
# WHY IT IS THIS SHORT
#
# This file previously pinned 30 packages, of which four were used. Among the
# unused: `neo4j`, on the same page as a README section citing PRD 04 NG3 —
# "Not a graph database application. Neo4j struck." — and a full authentication
# stack (pyjwt, python-jose, passlib) purchased against the threat model the
# README explicitly declines to defend against. It also pinned `torch`,
# `transformers`, `sentence-transformers`, `openai`, `qdrant-client`,
# `opensearch-py`, `mlflow`, `optuna`, `river`, `fastapi`, `vectorbt`, and
# `PyPortfolioOpt`, none of which any module imports.
#
# A dependency file is a claim about what the system needs. An inherited list
# nobody has read is a claim nobody has checked, and it is the reason `backtrader`
# stayed pinned here after the PRD struck it by name. The removals are recoverable
# from git history; re-add a line when an import needs it, not before.

numpy>=1.21.0

# The query engine over the bitemporal fact table (PRD §6). Imported by
# point_in_time.py, which degrades gracefully if it is absent.
duckdb>=1.0.0

# The backtest and execution engine the PRD names. Pinned and installed, and
# NOT YET IMPORTED by anything here — so FR-19 and FR-21 are unmet rather than
# delegated. It stays in this file because the decision to use it is made and
# the wiring is the outstanding work; see src/research_integrity/execution_costs.py.
nautilus_trader>=1.231.0

# --- test-only -------------------------------------------------------------
pytest>=7.0.0

# NOT LISTED, DELIBERATELY
#
# backtrader — PRD §3: "Whatever is chosen, Backtrader should be struck."
# neo4j      — PRD NG3: "Not a graph database application. Neo4j struck."
# mlflow     — the PRD names it for the experiment log; run_record.py implements
#              FR-22 through FR-25 over SQLite instead. That substitution is
#              flagged in the README and is owed a decision, so pinning the
#              library it replaced would misrepresent which one is in use.
#
# migration_inbox/finGuard carries its own requirements.txt and is V1 input,
# not a dependency of anything in src/.
